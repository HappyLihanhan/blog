# Project working rules

- Make and verify changes in the local workspace first.
- Treat GitHub Pages as the primary active hosting target. Public features must work under static hosting and must not require `/api/*` routes to render or perform their core interaction.
- Sites may be kept in sync when the task calls for it, but do not assume Sites is the primary runtime.
- Do not commit, push, or deploy unless the user explicitly requests it.
